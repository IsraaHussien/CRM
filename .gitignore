node_modules/
.env
.env.local
.next/
dist/
build/
*.log
.DS_Store
# Local Arabic Q&A log for this user — never committed, never uploaded (see CLAUDE.md's
# "Local Arabic Q&A log" section).
HOW-TO-USE.md
# Uploaded customer files (customer-management Story 7) — runtime data, not source.
backend/uploads/
.claude/scheduled_tasks.lock
# .squad/ is intentionally NOT ignored — its intake.md and plan files are part of
# the project's history and are meant to be committed.

# Managed by squad-kit — do not edit this block
.squad/secrets.yaml
.squad/stories/**/attachments/
.squad/.trash/
.squad/.last-run.json
.squad/.last-copy-prompt.md
.squad/runs/
# End squad-kit block
